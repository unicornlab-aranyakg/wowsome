const initialCharacterType = "CHAR::INITIAL_TYPE";

const initialState = {
  type: initialCharacterType,
  data: {}
};

export const actionCreators = {};
export const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
